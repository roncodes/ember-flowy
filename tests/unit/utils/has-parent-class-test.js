import hasParentClass from 'dummy/utils/has-parent-class';
import { module, test } from 'qunit';

module('Unit | Utility | has-parent-class', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = hasParentClass();
    assert.ok(result);
  });
});
