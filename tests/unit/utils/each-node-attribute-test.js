import eachNodeAttribute from 'dummy/utils/each-node-attribute';
import { module, test } from 'qunit';

module('Unit | Utility | each-node-attribute', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = eachNodeAttribute();
    assert.ok(result);
  });
});
