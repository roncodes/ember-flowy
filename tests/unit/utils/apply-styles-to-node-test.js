import applyStylesToNode from 'dummy/utils/apply-styles-to-node';
import { module, test } from 'qunit';

module('Unit | Utility | apply-styles-to-node', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = applyStylesToNode();
    assert.ok(result);
  });
});
