import eachNodeInput from 'dummy/utils/each-node-input';
import { module, test } from 'qunit';

module('Unit | Utility | each-node-input', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = eachNodeInput();
    assert.ok(result);
  });
});
